# requirements.txt
# ==============================================================
# Prediksi ML API - 依存パッケージ
# ==============================================================
# インストール: pip install -r requirements.txt
# ==============================================================

# Web フレームワーク
fastapi>=0.104.0          # API フレームワーク本体
uvicorn[standard]>=0.24.0 # ASGI サーバー（FastAPI を動かすサーバー）

# データバリデーション（FastAPI が内部で使用）
pydantic>=2.5.0

# ML / データ処理（ml_pipeline.py で使用）
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0             # モデルの保存・読み込み

# 開発用（オプション）
python-multipart>=0.0.6   # ファイルアップロード対応（将来の CSV 直接アップロード用）
